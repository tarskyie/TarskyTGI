<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="TarskyTGI.ChatPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TarskyTGI"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="300"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListBox Grid.Row="0" Grid.Column="0" x:Name="ChatHistory" Margin="0,0,10,0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBox Text="{Binding}" TextWrapping="Wrap" IsReadOnly="True" BorderThickness="0" Background="Transparent"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <Grid Grid.Row="1" Grid.Column="0" Margin="0,10,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="PromptBox" 
                     PlaceholderText="Type a message (Shift+Enter for new line)..."
                     PreviewKeyDown="PromptBox_KeyDown"
                     MinHeight="50"
                     MaxHeight="150"
                     TextWrapping="Wrap"
                     AcceptsReturn="True"
                     Grid.Column="0"/>

            <Button Content="Send" Click="SendFN" Style="{StaticResource AccentButtonStyle}" Height="50" Width="70" Margin="10,0,0,0" Grid.Column="1"/>
            <Button Content="Clear" Click="ClearFN" Height="50" Width="70" Margin="10,0,0,0" Grid.Column="2"/>
        </Grid>

        <ScrollViewer Grid.Column="1" Grid.RowSpan="2" VerticalScrollBarVisibility="Auto" Padding="10,0,0,0">
            <StackPanel Spacing="10">

                <TextBlock Text="Status" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <TextBox x:Name="StatusTextBlock" Text="Not loaded." IsReadOnly="True" Background="#11FFFFFF"/>

                <MenuFlyoutSeparator/>

                <TextBlock Text="Model Setup" Style="{StaticResource BodyStrongTextBlockStyle}"/>

                <Grid ColumnDefinitions="*,Auto">
                    <TextBox x:Name="ModelBox" PlaceholderText="Model path (.gguf)" TextChanged="ModelBox_TextChanged"/>
                    <Button x:Name="selectModelButton" Content="📁" Click="selectModelButton_Click" Grid.Column="1" Margin="5,0,0,0"/>
                </Grid>

                <Grid ColumnDefinitions="*,Auto">
                    <TextBox x:Name="MmprojBox" PlaceholderText="Multimodal (mmproj) path"/>
                    <Button x:Name="selectMmprojButton" Content="📁" Click="selectMmprojButton_Click" Grid.Column="1" Margin="5,0,0,0"/>
                </Grid>

                <ComboBox x:Name="ChatFormatCombo" Header="Chat Format" HorizontalAlignment="Stretch" SelectedIndex="0" IsEditable="True">
                    <x:String>chatml</x:String>
                    <x:String>llama-3</x:String>
                    <x:String>llava-1-5</x:String>
                    <x:String>llava-1-6</x:String>
                    <x:String>moondream2</x:String>
                    <x:String>nanollava</x:String>
                    <x:String>llama-3-vision-alpha</x:String>
                    <x:String>minicpm-v-2.6</x:String>
                    <x:String>obsidian</x:String>
                    <x:String>alpaca</x:String>
                    <x:String>vicuna</x:String>
                </ComboBox>

                <Grid ColumnDefinitions="*,*">
                    <TextBox x:Name="gpuLayers" Header="GPU Layers" Text="0" Margin="0,0,5,0"/>
                    <Button x:Name="loadbtn" Content="Load Model" Click="LoadModelButton_Click" HorizontalAlignment="Stretch" VerticalAlignment="Bottom" Grid.Column="1" Margin="5,0,0,0"/>
                </Grid>

                <MenuFlyoutSeparator/>

                <TextBlock Text="Vision Input" Style="{StaticResource BodyStrongTextBlockStyle}"/>
                <Grid ColumnDefinitions="*,Auto">
                    <TextBlock x:Name="uploadStatus" Text="No image selected." VerticalAlignment="Center" FontSize="12" TextTrimming="CharacterEllipsis"/>
                    <Button x:Name="uploadButton" Content="Upload Image" Click="uploadButton_Click" Grid.Column="1"/>
                </Grid>
                <Image x:Name="imagePreview" MaxHeight="150" HorizontalAlignment="Center" Stretch="Uniform"/>

                <MenuFlyoutSeparator/>

                <Expander Header="Generation Parameters" HorizontalAlignment="Stretch" IsExpanded="False">
                    <StackPanel Spacing="8">
                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Text="Context Size (n_ctx)" VerticalAlignment="Center"/>
                            <TextBox x:Name="ctxBox" Text="2048" Width="60" BeforeTextChanging="TextBox_BeforeTextChanging" Grid.Column="1" Margin="10,0,0,0"/>
                        </Grid>
                        <Slider x:Name="ctxSlider" Minimum="128" Maximum="8192" Value="2048" StepFrequency="128" Margin="0,5,0,0"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Text="Temperature" VerticalAlignment="Center"/>
                            <TextBox x:Name="temperatureBox" Text="0.8" Width="60" BeforeTextChanging="TextBox2_BeforeTextChanging" Grid.Column="1" Margin="10,0,0,0"/>
                        </Grid>
                        <Slider x:Name="temperatureSlider" Minimum="0" Maximum="2" Value="0.8" StepFrequency="0.05" Margin="0,5,0,0"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Text="Top P" VerticalAlignment="Center"/>
                            <TextBox x:Name="toppBox" Text="0.95" Width="60" BeforeTextChanging="TextBox2_BeforeTextChanging" Grid.Column="1" Margin="10,0,0,0"/>
                        </Grid>
                        <Slider x:Name="toppSlider" Minimum="0" Maximum="1" Value="0.95" StepFrequency="0.01" Margin="0,5,0,0"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Text="Min P" VerticalAlignment="Center"/>
                            <TextBox x:Name="minpBox" Text="0.05" Width="60" BeforeTextChanging="TextBox2_BeforeTextChanging" Grid.Column="1" Margin="10,0,0,0"/>
                        </Grid>
                        <Slider x:Name="minpSlider" Minimum="0" Maximum="1" Value="0.05" StepFrequency="0.01" Margin="0,5,0,0"/>

                        <Grid ColumnDefinitions="*,Auto">
                            <TextBlock Text="Typical P" VerticalAlignment="Center"/>
                            <TextBox x:Name="typicalpBox" Text="1" Width="60" BeforeTextChanging="TextBox2_BeforeTextChanging" Grid.Column="1" Margin="10,0,0,0"/>
                        </Grid>
                        <Slider x:Name="typicalpSlider" Minimum="0" Maximum="1" Value="1" StepFrequency="0.01" Margin="0,5,0,0"/>
                    </StackPanel>
                </Expander>

                <Expander Header="System Priority" HorizontalAlignment="Stretch" IsExpanded="False">
                    <ComboBox x:Name="priorityBox" HorizontalAlignment="Stretch" SelectionChanged="priorityBox_SelectionChanged" SelectedIndex="2">
                        <ComboBoxItem Content="Idle"/>
                        <ComboBoxItem Content="BelowNormal"/>
                        <ComboBoxItem Content="Normal"/>
                        <ComboBoxItem Content="AboveNormal"/>
                        <ComboBoxItem Content="High"/>
                        <ComboBoxItem Content="RealTime"/>
                    </ComboBox>
                </Expander>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>